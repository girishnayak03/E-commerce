server:
  port: 8084

spring:
  application:
    name: review-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/shopscale_review

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
